naftiko: 1.0.0-alpha2
info:
  label: Cribl Lake API — Datasets
  description: 'Cribl Lake API — Datasets. 5 operations. Lead operation: List all Lake datasets. Self-contained Naftiko capability
    covering one Cribl business surface.'
  tags:
  - Cribl
  - Datasets
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    CRIBL_API_KEY: CRIBL_API_KEY
capability:
  consumes:
  - type: http
    namespace: lake-datasets
    baseUri: https://{workspaceName}-{organizationId}.cribl.cloud/api/v1
    description: Cribl Lake API — Datasets business capability. Self-contained, no shared references.
    resources:
    - name: lake-datasets
      path: /lake/datasets
      operations:
      - name: listlakedatasets
        method: GET
        description: List all Lake datasets
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createlakedataset
        method: POST
        description: Create a Lake dataset
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: lake-datasets-id
      path: /lake/datasets/{id}
      operations:
      - name: getlakedataset
        method: GET
        description: Get a Lake dataset by ID
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: updatelakedataset
        method: PATCH
        description: Update a Lake dataset
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deletelakedataset
        method: DELETE
        description: Delete a Lake dataset
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.CRIBL_API_KEY}}'
  exposes:
  - type: rest
    namespace: lake-datasets-rest
    port: 8080
    description: REST adapter for Cribl Lake API — Datasets. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/lake/datasets
      name: lake-datasets
      description: REST surface for lake-datasets.
      operations:
      - method: GET
        name: listlakedatasets
        description: List all Lake datasets
        call: lake-datasets.listlakedatasets
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createlakedataset
        description: Create a Lake dataset
        call: lake-datasets.createlakedataset
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/lake/datasets/{id}
      name: lake-datasets-id
      description: REST surface for lake-datasets-id.
      operations:
      - method: GET
        name: getlakedataset
        description: Get a Lake dataset by ID
        call: lake-datasets.getlakedataset
        outputParameters:
        - type: object
          mapping: $.
      - method: PATCH
        name: updatelakedataset
        description: Update a Lake dataset
        call: lake-datasets.updatelakedataset
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deletelakedataset
        description: Delete a Lake dataset
        call: lake-datasets.deletelakedataset
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: lake-datasets-mcp
    port: 9090
    transport: http
    description: MCP adapter for Cribl Lake API — Datasets. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: list-all-lake-datasets
      description: List all Lake datasets
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: lake-datasets.listlakedatasets
      outputParameters:
      - type: object
        mapping: $.
    - name: create-lake-dataset
      description: Create a Lake dataset
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: lake-datasets.createlakedataset
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: get-lake-dataset-id
      description: Get a Lake dataset by ID
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: lake-datasets.getlakedataset
      outputParameters:
      - type: object
        mapping: $.
    - name: update-lake-dataset
      description: Update a Lake dataset
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: lake-datasets.updatelakedataset
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-lake-dataset
      description: Delete a Lake dataset
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: lake-datasets.deletelakedataset
      outputParameters:
      - type: object
        mapping: $.
